"""chemoinformatic-core service package.

Treat ``src/`` as the package root; configure your test runner / FastAPI app
to put this directory on the Python path (e.g. ``uvicorn main:app`` from
the ``src/`` directory, or ``rootdir = src`` in ``pyproject.toml``).

A future refactor restructures this as ``src/chemoinformatic_core/`` and
publishes a proper installable package (Phase 2 ADR).
"""
